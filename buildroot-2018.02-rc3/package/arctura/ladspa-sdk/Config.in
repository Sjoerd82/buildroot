config BR2_PACKAGE_LADSPA_SDK
	bool "ladspa-sdk"
	select BR2_INSTALL_LIBSDTCPP
	help
	  LADSPA is a standard that allows software audio processors and
	  effects to be plugged into a wide range of audio synthesis and
	  recording packages.

	  http://www.ladspa.org/
